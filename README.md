# Steps
- Clone the repo
- Install docker and docker compose and setup docker login.
- Add Docker Sudo Group
- Pull the following Docker image:
  ```
  docker pull thebloop/nav2-dev_container:latest
  ```
